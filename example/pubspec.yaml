name: example
description: flutter_html example app.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter_html:
    path: ..
  flutter_html_all:
    path: ../packages/flutter_html_all
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/html5.png
    - assets/mac.svg